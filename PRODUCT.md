# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

个人用户与学生群体：想把课程、讲座、B 站长视频快速变成笔记与知识档案的人。作者本人是第一位用户，同时把本项目作为求职面试的旗舰展示项目——代码质量、完成度与设计细节本身就是被评估的对象。

## Product Purpose

把任意视频变成一个可读、可检索、可分享的知识文档。粘贴 B 站链接或上传本地视频，应用自动完成下载 → 转写 → 画面理解（OCR + 关键帧）→ AI 结构化总结 → 思维导图，产出「总结文档 + 完整文字稿 + 可交互思维导图」三种形态。成功意味着：一条链接进去，几分钟内拿到一份能读、能查、能跳回视频原点的学习资料。

## Positioning

不绑定单一视频平台：B 站链接之外还能上传任意本地视频，这是 B 站自带「AI 视频总结」做不到的；在此之上叠加三合一闭环体验——总结文档、思维导图、导图节点点击跳转视频对应时间点，形成「读 → 看 → 回到原片」的完整学习路径。

## Operating Context

- Windows 桌面应用（Electron 35 + electron-vite），界面语言中文。
- AI 能力依赖硅基流动 API：转写 `FunAudioLLM/SenseVoiceSmall`，总结默认 `Qwen/Qwen2.5-7B-Instruct`（可在设置中更换）。
- 内置 yt-dlp 与 ffmpeg 二进制，用户无需自行安装。
- 所有数据存于本机 `%APPDATA%/video-summary/`（config.json / store.json / projects/<id>/）。
- 长视频按 10 分钟分段转写；关键帧数量受 `maxKeyFrames`（默认 30）限制以控制耗时。

## Capabilities and Constraints

**已确认能力**
- 两种来源：B 站链接自动下载（优先 H.264 mp4 音视频合并）／本地视频文件。
- 五阶段管线：下载 → 提帧∥音频分段转写 → 视觉分析（OCR + 画面描述）→ 总结 → 思维导图；每阶段实时进度、可取消。
- 断点续跑：各阶段产物落盘，失败/取消后一键继续；模型或参数变更时自动重做相关阶段。
- 总结文档：章节卡片（概述 + 要点 + 子要点）、全片核心要点（takeaways）、章节关联截图与 OCR 文字。
- 思维导图：React Flow 画布，最多 6 层，节点带时间区间，点击跳转内置播放器对应时间点；支持重新布局、导入/导出、重新生成。
- 浅色/深色主题全量适配。

**硬约束（不可妥协）**
- 本地优先：数据只存本机，API Key 不上传任何第三方（仅随模型请求发往硅基流动）。
- Windows 桌面形态长期存在，不做网页版。
- 中文内容优先。

**明确未决**
- 打包分发方案（`npm run package:win` 标注 W4 完善中）。

## Brand Commitments

- 名称：Video Summary（视频总结助手）。
- 无 logo、无既定视觉资产承诺。

## Evidence on Hand

- README.md（260 行）：完整功能清单、技术栈、目录结构、使用说明与已修复问题清单。
- 真实运行数据在 `%APPDATA%/video-summary/projects/`（含真实转写、总结、导图产物）。
- 无营销素材、无用户见证、无基准测试数据——后续任何文案不得虚构这些内容。

## Product Principles

1. **本地优先**：一切数据与密钥留在本机；联网仅限模型调用与视频下载，且对用户透明。
2. **一键全自动**：粘贴链接到拿到文档+导图，中间不需要人工干预；等待过程可见、可断点续跑。
3. **内容闭环**：读文档 → 看导图 → 跳回视频原点，学习路径不断裂。
4. **平台中立**：不依赖任何单一视频平台的私有能力，本地文件与在线链接平权。
5. **展示级完成度**：作为面试旗舰项目，工程质量、细节打磨与真实可用性是产品的一部分，不是附加项。
